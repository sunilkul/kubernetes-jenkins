apiVersion: v1
kind: Service
metadata:
  name: {{ include "springboot-k8s-helm.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "springboot-k8s-helm.name" . }}